# Timeline curation schedule prompt

Use the Timeline Curator skill to run one complete curation cycle for my authenticated Timeline account. Retrieve the latest policy and verify that a daily run remains before doing any research. If quota is exhausted, stop and report its reset time. Otherwise, use your own web/search/browser capabilities, prefer authoritative evidence, publish only high-confidence story clusters, and complete the run even when no stories qualify. Never use an LLM API or shared scraping credential. Never ask for or provide a tenant ID.

Recommended personal schedules: 07:00 and 18:00 in the user's own timezone. Each trigger starts a fresh task; the Timeline context is the durable state.
